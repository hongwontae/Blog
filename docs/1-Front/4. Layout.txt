1. flex
1-1. flex-wrap
=> 아이템들이 컨테이너 너비를 초과하면 다음 줄로 내려가 배치됩나다.'




2. clamp()
2-1. clamp(MIN, IDEAL, MAX)
=> 최소값, 이상적인 값, 최대값 인자를 받습니다.

2-2. IDEL를 계산 -> MIN/MAX 제한 -> 최종값 적용
=> w-[clamp(150px,50%,300px)]
=> W의 50% 계산 값이 100px이라면 150px 적용, 400px이라면 300px 적용 150px ~ 300px 이내라면 해당 값을 적용하는
 매커니즘입니다.

